{
    "title": "Scrapy Executor",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "keyword": {
            "title": "Keyword",
            "type": "string",
            "description": "Specify a keyword to search for the quote.",
            "prefill": "You're gonna need a bigger boat.",
            "default": "",
            "editor": "textfield"
        },
        "Testing": {
                    "title": "Testing",
                    "type": "boolean",
                    "description": "Enable testing mode for scraping only 50 movies.",
                    "default": false
        },
        "proxyConfig": {
            "title": "Proxy",
            "type": "object",
            "description": "Proxy servers let you bypass website protections, avoid IP address blocking and view content for other countries. Try to use the proxy if you are experiencing timeout errors.",
            "default": {},
            "editor": "proxy"
        }
    },
    "required": ["Keyword"]
}
