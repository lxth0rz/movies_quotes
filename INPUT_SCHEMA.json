{
    "title": "Scrapy Executor",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "keyword": {
            "title": "Keyword",
            "type": "string",
            "description": "Specify a keyword(s) to search for the quote.",
            "prefill": "May the Force be with you.",
            "default": "",
            "editor": "textfield"
        },
        "First 1000 Only": {
                    "title": "First1000",
                    "type": "boolean",
                    "description": "Only extract quotes from first 1000 movies then stops the actor.",
                    "default": false
        },
        "proxyConfig": {
            "title": "Proxy",
            "type": "object",
            "description": "Proxy servers let you bypass website protections, avoid IP address blocking and view content for other countries. Try to use the proxy if you are experiencing timeout errors.",
            "default": {},
            "editor": "proxy"
        }
    },
    "required": ["keyword"]
}
